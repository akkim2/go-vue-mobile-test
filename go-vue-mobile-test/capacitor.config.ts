import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.akkim2.govuemobiletest',
  appName: 'go-vue-mobile-test',
  webDir: 'dist'
};

export default config;
